<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/subSubject.css">
    <title>Document</title>
</head>
<body>

    <nav>
        <h1 class="logo">JsSmartExam</h1>
        <ul>
            <li>Home</li>
            <li>Quizzes</li>
            <li>Subjects</li>
        </ul>
        <button class="signOut">Sign out</button>
    </nav>

    <div style=" display: flex; flex-wrap: wrap; justify-content: center; align-items: center; justify-content: space-evenly; gap: 5px; background-color: rgb(255, 255, 255); width: 80%; margin-left: auto; margin-right: auto; padding: 100px 30px; margin-top: 90px; box-shadow: 0px 3px 20px rgb(0, 153, 255);">
       <div class="subject">
        <h1 style="color: black; text-align: center;"><%= subject.name %></h1>
        <div class="floating-stack">
          <dl>
            <% if(subSubjects.length > 0) { %>
                <% subSubjects.forEach(subSubject => { %>
                    <dd><%= subSubject.name %></dd>
                <% }) %>
           <% } else { %>
            <div style="background-color: rgb(245, 95, 95); padding: 20px; margin-top: 40px; text-align: center; border: none; border-radius: 0px;">
                <h1 style="color: rgb(255, 255, 255); font-size: 29px;">THERE IS NO SUB SUBJECT YET</h1>
            </div>
            <% } %>
          </dl>
        </div>
      </div>
        <div class="line"></div>
        <div class="subSubject">    
            <h1>Add Sub Subject</h1>
            <form action="/subject/addSubSubject" method="post">
                <div class="form">
                    <label for="">Name</label>
                    <input type="text" name="name">
                    <input type="hidden" name="subSubject_id" value="<%= subject.id %>">
                </div>
                <button class="send">Add</button>
            </form>
        </div>
    </div>

</body>
</html>